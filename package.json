{
  "name": "TaskFlow",
  "private": true,
  "workspaces": [
    "frontend",
    "backend/services/*",
    "backend/shared"
  ],
  "scripts": {
    "build:lambdas": "npm run build --workspaces --if-present && ./scripts/package-lambdas.sh"
  }
}
