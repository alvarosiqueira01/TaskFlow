{
  "name": "@taskmanager/collaboration",
  "version": "1.0.0",
  "description": "collaboration microservice",
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild src/index.js --bundle --platform=node --target=node22 --outfile=dist/index.js",
    "test": "jest"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
